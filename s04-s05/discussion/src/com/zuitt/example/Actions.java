package com.zuitt.example;

public interface Actions {

    // Abstraction is a process where all the logic and complexity are hidden from the user. Instead, the only interaction of the user is on the things they understand.

    // Interfaces can act as contracts wherein that when a class implements the interface, it should have the methods the interface has defined in the class.

    public void sleep();
    public void run();
}
